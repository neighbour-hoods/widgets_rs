{
  "dependencies": {
    "@holochain/client": "file:submodules/holochain-client-js",
    "esbuild": "^0.14.54"
  },
  "scripts": {
    "setup": "npm install && npm install ./submodules/holochain-client-js",
    "serve": "miniserve -p 8080 .",
    "fe:build": "wasm-pack build --target web && ./node_modules/.bin/esbuild ./main.js --format=iife --bundle --outfile=./pkg/bundle.js",
    "fe:hc-js-build": "node esbuild.config.js"
  }
}
