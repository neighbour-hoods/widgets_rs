{
  "dependencies": {
    "@holochain/client": "file:submodules/holochain-client-js",
    "esbuild": "^0.14.54"
  },
  "scripts": {
    "setup": "npm install && npm install ./submodules/holochain-client-js",
    "serve": "miniserve -p 8080 .",
    "fe:build": "wasm-pack build --target web && rollup ./main.js --format iife --file ./pkg/bundle.js"
  }
}
